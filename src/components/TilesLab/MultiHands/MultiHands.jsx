import React, { PureComponent } from 'react';
import PropTypes from 'prop-types';
import './MultiHands.css';

export default class MultiHands extends PureComponent {
  static propTypes = {};

  render() {
    const { stateOfTiles } = this.props;
    return <div />;
  }
}
